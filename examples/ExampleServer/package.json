{
  "name": "@ep2/example-server",
  "private": true,
  "main": "out/index.js",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "postinstall": "npm run vapid",
    "start": "node out/ExampleServer/src/index.js",
    "vapid": "../../node_modules/.bin/web-push generate-vapid-keys --json > ./vapidKeys.test.json",
    "dev": "nodemon --watch src -e ts --exec 'npm run build && npm run start'"
  },
  "license": "MIT",
  "dependencies": {
    "@ep2/offline-server": "file:../../packages/offline-server",
    "@ep2/online-server": "file:../../packages/online-server"
  },
  "description": "",
  "$schema": "https://json.schemastore.org/package",
  "devDependencies": {
    "nodemon": "^2.0.22",
    "parcel": "^2.8.3"
  }
}